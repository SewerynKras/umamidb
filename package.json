{
  "name": "umami-golem-sync",
  "version": "1.0.0",
  "description": "Sync Umami analytics data to Golem DB",
  "main": "golem-sync.js",
  "scripts": {
    "sync": "node golem-sync.js sync",
    "query": "node golem-sync.js query",
    "sync-hourly": "node golem-sync.js sync",
    "install-deps": "bun install"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "golem-base-sdk": "^0.1.0",
    "pg": "^8.11.0",
    "tslib": "^2.8.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}